# dependencies
node_modules/

# next.js
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# environment — never commit credentials or project ids
.env
.env.local
.env*.local

# deployment
.vercel

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# macOS. This project lives on an exFAT volume, where Finder writes an
# AppleDouble sidecar next to every single file — 600+ of them. Without this
# they end up in the tree.
.DS_Store
._*

# editor / tooling scratch
.claude/
.playwright-mcp/
*.pem

# generated by `next dev`, not source
AGENTS.md
CLAUDE.md
